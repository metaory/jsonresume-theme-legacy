{{#if certificates.length}}
  {{> components/hr.hbs size="md" }}
  <section class="section">
    <header class="section-header">
      <h2 class="section-title grid-column-flow">
        <span class="icon {{ ICO "certificate" }}"></span>
        {{ TITLE "certificates" }}
      </h2>
    </header>
    <section id="certificates">
      {{#each certificates}}
        <section class="certificate-item">
          <header class="item-header">
            <div>
              {{#if name}}
                <div class="name content-title text-title-font">{{ name }}</div>
              {{/if}}
              {{#if issuer}}
                <div class="issuer content-title text-title-font">{{ issuer }}</div>
              {{/if}}
            </div>
            {{#if date}}
              <div class="date content-meta">{{ date }}</div>
            {{/if}}
          </header>
          <div class="item">
            {{#if url}}
              <span class="url">
                <span class="icon {{ ICO "link" }}"></span>
                <a target="_blank" href="{{ url }}">{{ URL url }}</a>
              </span>
            {{/if}}
          </div>
        </section>
      {{/each}}
    </section>
  </section>
{{/if}}
